# Project Summary

- Introduced a new architecture centered around a canonical `standards` source (`skills`, `workflows`, `mcp`) and a cross-agent coordinator CLI.
- Added `packages/coordinator-cli` (Node.js, ESM) with v1 commands: `init`, `link`, `sync`, `doctor`, `list`, `update`, `priority`, `backup`, `restore`, `remove`.
- Implemented adapter-target mapping for `claude`, `codex`, `qwen`, and `antigravity`.
- Added cross-platform setup scripts and compatibility matrix documentation.
- Established a priority model and state/backup mechanism under `.ai-coordinator` for deterministic, recoverable operations.
